Optional: put downloaded LottieFiles .json animations here for OFFLINE use.
Then in media-config.js point to them locally, e.g.:
  see1: { type:"lottie", src:"lottie/coffee.json" }

A lottie.host URL also works, but needs internet. Local files do not.
